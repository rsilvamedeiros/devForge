import{describe,expect,it}from'vitest';import{REACT_AREAS}from'./react-curriculum';describe('React curriculum',()=>{it('covers fifteen unique competency areas',()=>{expect(REACT_AREAS).toHaveLength(15);expect(new Set(REACT_AREAS.map(area=>area.id)).size).toBe(15)});it('contains evidence and progressive levels',()=>{expect(new Set(REACT_AREAS.map(area=>area.level)).size).toBe(3);expect(REACT_AREAS.every(area=>area.topics.length>=3&&area.evidence.length>15)).toBe(true)})});
