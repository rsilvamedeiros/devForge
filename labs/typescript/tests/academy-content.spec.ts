import{describe,expect,it}from'vitest';import{CHALLENGES,DOCUMENTS,QUESTIONS,TRACKS}from'../src/academy/academy-content.js';
describe('TypeScript Academy content',()=>{it('defines a progressive twelve-track curriculum',()=>{expect(TRACKS).toHaveLength(12);expect(new Set(TRACKS.map(track=>track.level))).toEqual(new Set(['Fundamentos','Intermediário','Avançado']));expect(TRACKS.every(track=>track.topics.length>=4)).toBe(true)});it('provides documentation, challenges and assessments',()=>{expect(DOCUMENTS.length).toBeGreaterThanOrEqual(20);expect(CHALLENGES).toHaveLength(12);expect(QUESTIONS).toHaveLength(9);expect(CHALLENGES.every(challenge=>challenge.solutionPattern instanceof RegExp)).toBe(true)})});
