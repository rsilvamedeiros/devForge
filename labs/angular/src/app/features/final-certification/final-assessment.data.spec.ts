import{FINAL_PASS_RATIO,FINAL_QUESTIONS}from'./final-assessment.data';describe('final assessment',()=>{it('covers progressive levels and has a senior majority',()=>{expect(new Set(FINAL_QUESTIONS.map(q=>q.level)).size).toBe(3);expect(FINAL_QUESTIONS.filter(q=>q.level==='Sênior').length).toBeGreaterThanOrEqual(4)});it('has valid answers and an 80 percent gate',()=>{expect(FINAL_PASS_RATIO).toBe(.8);expect(FINAL_QUESTIONS.every(q=>q.answer>=0&&q.answer<q.options.length&&q.explanation.length>20)).toBeTrue()})});
