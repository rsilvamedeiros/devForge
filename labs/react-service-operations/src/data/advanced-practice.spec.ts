import{describe,expect,it}from'vitest';import{CAPSTONE_SPRINTS,CAPSTONE_TASK_COUNT}from'./capstone-sprints';import{SANDBOX_WORKSPACES}from'./sandbox-workspaces';describe('advanced React practice',()=>{it('offers progressive multi-file workspaces',()=>{expect(SANDBOX_WORKSPACES.map(item=>item.level)).toEqual(['Júnior','Pleno','Sênior']);expect(SANDBOX_WORKSPACES.every(item=>item.files.length===4&&item.checks.length===4)).toBe(true)});it('defines a complete six-sprint capstone',()=>{expect(CAPSTONE_SPRINTS).toHaveLength(6);expect(CAPSTONE_TASK_COUNT).toBe(18);expect(new Set(CAPSTONE_SPRINTS.flatMap(item=>item.tasks).map(task=>task.id)).size).toBe(18)})});
