import{describe,expect,it}from'vitest';import{COMPONENT_RECIPES}from'./component-recipes';import{PLAYGROUND_EXERCISES}from'./playground-exercises';describe('React learning tools',()=>{it('offers a broad live catalog',()=>{expect(COMPONENT_RECIPES.length).toBeGreaterThanOrEqual(12);expect(new Set(COMPONENT_RECIPES.map(item=>item.category)).size).toBeGreaterThanOrEqual(6)});it('provides progressive executable exercises',()=>{expect(PLAYGROUND_EXERCISES.map(item=>item.level)).toContain('Sênior');expect(PLAYGROUND_EXERCISES.every(item=>item.starter.includes('function solve'))).toBe(true)})});
