import{describe,expect,it}from'vitest';import{ASSESSMENT_QUESTIONS,TECHNICAL_QA}from'./assessment-bank';import{CODING_CHALLENGES}from'./coding-challenges';describe('React assessments',()=>{it('covers three seniority levels evenly',()=>{for(const level of['Júnior','Pleno','Sênior'])expect(ASSESSMENT_QUESTIONS.filter(item=>item.level===level)).toHaveLength(5)});it('provides technical answers and signals',()=>{expect(TECHNICAL_QA).toHaveLength(12);expect(TECHNICAL_QA.every(item=>item.signals.length>=3&&item.answer.length>60)).toBe(true)});it('has public and hidden coding tests',()=>{expect(CODING_CHALLENGES).toHaveLength(5);expect(CODING_CHALLENGES.every(item=>item.tests.some(test=>test.hidden)&&item.tests.some(test=>!test.hidden))).toBe(true)})});
