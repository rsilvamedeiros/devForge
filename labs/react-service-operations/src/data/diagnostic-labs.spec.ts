import{describe,expect,it}from'vitest';import{DEBUG_CASES,PERFORMANCE_MISSIONS}from'./diagnostic-labs';
describe('diagnostic labs',()=>{it('offers progressive debugging incidents',()=>{expect(DEBUG_CASES).toHaveLength(6);expect(new Set(DEBUG_CASES.map(item=>item.level))).toEqual(new Set(['Júnior','Pleno','Sênior']));expect(DEBUG_CASES.every(item=>item.verification.length===3)).toBe(true)});it('requires measurable performance targets',()=>{expect(PERFORMANCE_MISSIONS).toHaveLength(6);expect(PERFORMANCE_MISSIONS.every(item=>item.target.render<=item.baseline.render&&item.target.bundle<=item.baseline.bundle&&item.target.interaction<=item.baseline.interaction)).toBe(true)})});
